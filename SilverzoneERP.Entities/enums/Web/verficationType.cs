namespace SilverzoneERP.Entities
{
    // for all enums we suffix with type > so that we can know this is a enum type
    public enum verficationType          // use to know mobile no use for regisration/forget password/ change paswrd & send the message acording th them 
    {
        register,
        forget,
        change        
    }
}
